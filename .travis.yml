# .travis.yml
language: node_js
node_js:
  - 10
install:
  - cd ./jest-demo
  - npm install
script:
  - npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: jest-demo
  repo: JasonXDDD/CICD-Demo